# @kale/stylelint-config

Run `npx @kale/stylelint-config` for init `.vscode/settings.json`

## init

```sh
npx @kale/stylelint-config
```

## config

create a `stylelint.config.[cm]js`:

```ts
import { defineConfig } from '@kale/stylelint-config'

export default defineConfig()
```
